
// Capacitor configuration disabled for web-only mode
/*
import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.remoterebooter.app',
  appName: 'MQTT Router Resetter',
  webDir: 'dist',
  server: {
    androidScheme: 'https'
  }
};

export default config;
*/
export default {};
